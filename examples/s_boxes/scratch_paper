a1b1 + a1c1 + c1 + a1b2 + a1c2
    = (a + r0)*(b + r1) + (a + r0)*(c + r2) + (c + r2) + (a + r0)*(r1) + (a + r0)*(r2)

Q:
    (a + r0)*(b + r1) + (a + r0)*(c + r2) + (c + r2) + (a + r0)*(r1) + (a + r0)*(r2)
    ==>
    (b + c) * (a + r0) + c + r2
    ==>
    r2

    sol: f = r0, r1, (b + c) * (a + r0) + c + r2 + (b' + c') * (a' + r0) + c'
